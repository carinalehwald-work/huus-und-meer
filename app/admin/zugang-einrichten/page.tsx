import Link from "next/link"; import { setupAccess } from "./actions";
const input="mt-1 min-h-11 w-full rounded-xl border border-mist-line bg-ice px-3";
export default async function Setup({searchParams}:{searchParams:Promise<{hinweis?:string}>}){const q=await searchParams;return <main className="grid min-h-dvh place-items-center bg-mist p-5"><form action={setupAccess} className="w-full max-w-md rounded-2xl bg-ice p-6 shadow-sm"><h1 className="text-2xl font-semibold">Zugang einrichten</h1><p className="mt-2 text-sm text-ink/70">Lege mit E-Mail und deinem einmaligen Einrichtungscode dein eigenes Passwort fest.</p>{q.hinweis?<p className="mt-4 rounded-xl bg-mist p-3 text-sm">{q.hinweis}</p>:null}<div className="mt-5 grid gap-4"><label>E-Mail<input className={input} name="email" required type="email"/></label><label>Einrichtungscode<input className={input} name="code" required/></label><label>Neues Passwort (mindestens 12 Zeichen)<input className={input} minLength={12} name="password" required type="password"/></label><button className="min-h-11 rounded-xl bg-brand px-4 font-semibold text-ice">Zugang einrichten</button></div><Link className="mt-4 inline-block text-sm font-semibold text-brand" href="/admin/login">Zur Anmeldung</Link></form></main>}
